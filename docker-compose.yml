services:
  db:
    image: postgres:15
    restart: always
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=Shodmon123
      - POSTGRES_DB=fileupload
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
  web:
    build: .
    restart: always
    ports:
      - "8080:8000"
    env_file:
      - .env
    volumes:
      - .:/app

volumes:
  pgdata: